# Copy this file to keystore.properties and fill in your values.
# keystore.properties is gitignored — never commit it.
#
# To generate a keystore:
#   keytool -genkey -v -keystore planespotter-release.keystore \
#     -alias planespotter -keyalg RSA -keysize 2048 -validity 10000
#
storeFile=../planespotter-release.keystore
storePassword=
keyAlias=planespotter
keyPassword=
